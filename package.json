{
  "scripts": {
    "prepare":    "husky install",
    "test":       "echo \"No root-level tests\" && exit 0",
    "lint":       "echo \"No root-level lint\" && exit 0",
    "type-check": "echo \"No root-level type-check\" && exit 0",
    "auto-commit": "node scripts/auto-commit.js",
    "auto-pr":    "node scripts/auto-pr.js",
    "auto-jira":  "node scripts/auto-jira.js",
    "dashboard":  "node scripts/dashboard.js"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "minimist": "^1.2.8"
  }
}
